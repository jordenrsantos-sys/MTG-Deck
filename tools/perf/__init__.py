"""Performance instrumentation + audit tooling (Phase 6 Stage 6).

Module marker. See `timing_instrumentation_v1.py` for the TimingProbe
context manager and `run_perf_timing_audit.py` for the canonical CLI
that emits PERF_TIMING_REPORT_<date>.md.
"""
