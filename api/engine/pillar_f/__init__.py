"""Pillar F — playtest substrate.

v0.1 statistical approximator lives at `api/engine/layers/agent_statistical_approximator_v1.py`.
v0.2 rules-correct multiplayer game engine lives under `pillar_f/v0_2/`.
Both ship simultaneously; v0.2 will eventually feed Stage 2 graduated
playtest while v0.1 remains the cheap Stage 1 baseline.
"""
